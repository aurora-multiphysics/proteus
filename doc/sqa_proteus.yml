directories:
    - ${ROOT_DIR}/test/tests
specs:
    - tests
dependencies:
    - framework
repos:
    default: https://github.com/aurora-multiphysics/proteus (push)
reports: !include ${ROOT_DIR}/doc/sqa_reports.yml
